export interface Device {
    id: number
    name: string
    price: string
    rating: number
    img: string
}

export interface TypeOrBrand {
    id: number
    name: string
}

export interface Info {
    id?: number
    title: string
    description: string
}